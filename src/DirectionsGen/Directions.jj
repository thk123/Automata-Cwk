options {	STATIC = false;	IGNORE_CASE = true;
}PARSER_BEGIN(Directions)	class Directions	{		public static void main(String[] args) throws ParseException		{			Directions parser = new Directions(System.in);			parser.Start();		}	}PARSER_END(Directions)void Start() :{}{  < PLACESTART > (< WHITESPACE >) <PLACE > (< WHITESPACE >) "is" (< WHITESPACE >) < DIRECTION > (< WHITESPACE >) "of" (< WHITESPACE >) < PLACESTART > (< WHITESPACE >) <PLACE > (< WHITESPACE >)*  <EOF >}TOKEN :{  < DIRECTION : "NORTH" | "SOUTH" | "EAST" | "WEST" >}TOKEN :{ < PLACESTART : "THE" > }TOKEN :{  < PLACE : (["a" - "z"])+ >}TOKEN :{  < WHITESPACE : ("\r" | "\n" | " ") >}	  
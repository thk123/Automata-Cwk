options {	STATIC = false;	IGNORE_CASE = true;
}PARSER_BEGIN(Directions)	class Directions	{		public static void main(String[] args) throws ParseException		{			Directions parser = new Directions(System.in);			parser.Start();		}	}PARSER_END(Directions)void Start() :{}{  < PLACEDEFINITION > (< WHITESPACE >) "is" (< WHITESPACE >) < DIRECTION > (< WHITESPACE >) "of" (< WHITESPACE >) < PLACEDEFINITION > (< WHITESPACE >)*  <EOF >}TOKEN :{  < DIRECTION : "NORTH" | "SOUTH" | "EAST" | "WEST" >}TOKEN :{  < PLACEDEFINITION : < PLACESTART > (< WHITESPACE >)+ < PLACE > >}TOKEN :{ < PLACESTART : "THE" > }TOKEN :{  < PLACE : (["a" - "z"])+ >}TOKEN :{  < WHITESPACE : ("\r" | "\n" | " ")+ >}	  